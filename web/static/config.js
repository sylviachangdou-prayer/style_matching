// API base URL. Leave empty when the FastAPI app serves this page itself
// (local dev, or the HF Space). Before deploying to Vercel, point it at the
// Hugging Face Space, e.g.:
// window.STYLEMATCH_API = "https://YOUR-USERNAME-stylematch.hf.space";
window.STYLEMATCH_API = "";

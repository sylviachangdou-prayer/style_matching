// API base URL. Leave empty when the FastAPI app serves this page itself
// (local dev, or the HF Space). Before deploying to Vercel, point it at the
// Hugging Face Space, e.g.:
// window.STYLEMATCH_API = "https://YOUR-USERNAME-stylematch.hf.space";
window.STYLEMATCH_API = "";

// URL printed (with a QR code) on exported PDF/image result sheets. Leave unset
// to use the page's own address; set it to the canonical public URL before
// deploying behind a proxy or preview domain, e.g.:
// window.STYLEMATCH_SHARE_URL = "https://stylematch.example.com";

